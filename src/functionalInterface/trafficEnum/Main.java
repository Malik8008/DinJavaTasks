package functionalInterface.trafficEnum;

public class Main {
    public static void main(String[] args) {
        AboutTrafficLight.about(TrafficLight.RED);
        AboutTrafficLight.about(TrafficLight.YELLOW);
        AboutTrafficLight.about(TrafficLight.GREEN);
    }
}
